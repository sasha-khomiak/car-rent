import React from 'react';

import { StyledFooterSection, StyledFooterContainer } from './Footer.styled';

const Footer = () => {
  return (
    <StyledFooterSection>
      <StyledFooterContainer>!!!</StyledFooterContainer>
    </StyledFooterSection>
  );
};

export default Footer;
